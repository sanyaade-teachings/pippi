#ifndef LPASTRID
#define LPASTRID

int lprendernode_init(int argc, char * argv[]);

/*
enum NodeMessage {
    LPNODE_PLAY,
    NUM_NODEMESSAGES
};

typedef struct lpmsgnode_t * {
    enum NodeMessage msg;
    void * next;
}

typedef struct lpmsgbus_t {
    lpmsgnode_t * head;
} lpmsgbus_t;

typedef struct lpbufnode_t * {
    lpbuffer_t * buf;
    void * next;
} lpbufnode_t;

typedef struct lpbufnodebus_t {
    lpbufnode_t * head;
} lpbufnodebus_t;
*/

#endif
